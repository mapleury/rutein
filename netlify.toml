[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_GEOCODING_PROVIDER,VITE_ROUTING_PROVIDER"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200